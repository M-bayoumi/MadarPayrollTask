@model IEnumerable<Payroll_Mohamed_Bayoumi.Models.Salary>

<div class="container mt-5">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white text-center">
            <h2>Salaries</h2>
        </div>
        <div class="card-body">
            <table class="table table-bordered table-striped table-hover text-center">
                <thead class="table-dark">

                    <tr>
                        <th class="col-2">Amount</th>
                        <th class="col-4">Job Grade</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var salary in Model)
                    {
                        <tr class="col-">
                            <td>@salary.Amount</td>
                            <td>@salary.JobGrade</td>
                            <td class="text-center">
                                <a asp-action="Details" asp-route-id="@salary.Id" class="btn btn-info btn-sm px-4">Details</a>
                                <a asp-action="Edit" asp-route-id="@salary.Id" class="btn btn-warning btn-sm px-4 ms-2">Edit</a>
                                <a asp-action="Delete" asp-route-id="@salary.Id" class="btn btn-danger btn-sm px-4 ms-2">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <div class="text-center mt-3">
                <a asp-action="Create" class="btn btn-success btn-lg px-5">Add New Department</a>
            </div>
        </div>
    </div>
</div>
